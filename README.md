# scoop-bucket

Scoop bucket for [inference.sh](https://inference.sh) CLI tools.

## Install

```powershell
scoop bucket add inference https://github.com/inference-sh/scoop-bucket
scoop install belt
```
